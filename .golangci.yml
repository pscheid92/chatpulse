version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - durationcheck
    - errname       # Sentinel errors must be named Err*
    - errorlint     # Check error wrapping (use %w, not %v)
    - fatcontext
    - goconst
    - gocritic
    - misspell
    - nilerr
    - noctx
    - prealloc
    - unconvert
    - unparam
    - wastedassign
    - wrapcheck     # Check errors are wrapped at appropriate layers
  settings:
    goconst:
      min-len: 3
      min-occurrences: 3
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
      disabled-checks:
        - hugeParam        # structs passed by value are fine for our sizes
        - rangeValCopy     # copying small structs in range is fine
        - exitAfterDefer   # standard pattern in main() and TestMain()
    unparam:
      check-exported: false
    errcheck:
      exclude-functions:
        # Close/Rollback errors are generally ignorable for cleanup
        - (io.ReadCloser).Close
        - (github.com/jackc/pgx/v5.Tx).Rollback
        - (*github.com/gorilla/websocket.Conn).Close
        - (*github.com/gorilla/websocket.Conn).SetWriteDeadline
        - (*github.com/gorilla/websocket.Conn).SetReadDeadline
  exclusions:
    paths:
      - internal/database/sqlcgen
    rules:
      # Deprecated middleware.Logger — replacing requires larger refactor
      - linters: [staticcheck]
        text: "SA1019: middleware.Logger"
      # bodyclose false positive on WebSocket Dial in tests
      - linters: [bodyclose]
        path: _test\.go

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true

output:
  sort-order:
    - file
